# Aquarium

터미널 속 작은 힐링 어항. ASCII 열대어들이 헤엄치고, 공기방울이 올라오고, 시간이 지나면 아기 물고기가 태어납니다.

![어항 데모 — 물고기들이 헤엄치고, 먹이를 주면 몰려들고, 아기 물고기가 태어납니다](demo.gif)

## 설치

### Homebrew (권장)

```sh
brew install agiletalk/tap/aquarium
aquarium
```

### 미리 빌드된 바이너리 (Swift 불필요, macOS 전용)

Apple Silicon과 Intel Mac을 모두 지원하는 유니버설 바이너리입니다:

```sh
curl -L https://github.com/agiletalk/Aquarium/releases/latest/download/aquarium.tar.gz | tar xz
./aquarium
```

### 소스에서 빌드 (Swift 5.9+)

```sh
swift run -c release
```

또는 빌드 후 바이너리 직접 실행:

```sh
swift build -c release
.build/release/aquarium
```

## 조작

| 키 | 동작 |
|----|------|
| `f` | 먹이 주기 — 물고기들이 먹이를 향해 몰려듭니다 |
| `g` | 생먹이(브라인슈림프) 방류 — 도망다니는 새우 떼와 추격전이 벌어집니다 |
| `i` | 도감 열기/닫기 — 물고기 이름·나이·먹은 먹이 수, 손님 기록 |
| `n` | 조명 전환 — 자동 → 밤 → 낮 순환 |
| `m` | 음악 켜기/끄기 — DOS 감성 칩튠 플레이리스트 (lo-fi 코딩용) |
| 마우스 클릭 | 물고기 만지기 — "○○를 만졌어요!" 뽁 소리와 함께 화들짝 도망갑니다 |
| `q` / `Ctrl-C` | 종료 (어항 자동 저장) |

## 어항 요약 한 줄 (`--status`)

어항을 켜지 않고도 상태를 확인할 수 있습니다:

```sh
$ aquarium --status
><> 12마리 · 7일째 · 다음 탄생까지 2분
```

tmux 상태바에 어항을 넣으려면 `~/.tmux.conf`에:

```
set -g status-right '#(aquarium --status)'
set -g status-interval 60
```

`--help`, `--version`도 지원합니다.

## 구경 포인트

- 물고기마다 색과 종이 다릅니다. 줄무늬 열대어(`><|||°>`)도 있어요.
- 공기방울은 수초와 물고기 입에서 올라오며, 수면에 가까워질수록 커집니다 (`.` → `o` → `O`).
- 3~5분마다 아기 물고기(`><>`)가 태어나고, 자라면 어른 물고기가 됩니다.
- 먹이를 자주 주면 번식이 빨라집니다. 최대 마릿수는 터미널 크기에 비례해요.
- 바닥의 보물상자 `[____]`가 이따금 덜컥 열리며 금화를 반짝이고 공기방울을 뿜습니다. 근처 물고기들은 깜짝 놀라 흩어져요.
- 해파리 `(___)`가 갓을 오므렸다 펴며 둥실둥실 떠다닙니다. 오므릴 때 위로 쏘옥, 펴면 천천히 가라앉아요.
- 터미널 크기를 조절하면 어항도 실시간으로 따라 커지고 작아집니다.
- **BGM이 나옵니다.** `m`을 누르면 DOS 시절 게임 음악 같은 칩튠 플레이리스트가 흘러나옵니다 — 「물속 산책」「달빛 어항」「산호초 왈츠」「새우 행진곡」 4곡 순환. 외부 파일 없이 사각파+삼각파를 실시간 합성하는 내장 신시사이저라서 어디서든 바로 나와요. 터미널에 어항 띄워두고 lo-fi 삼아 코딩하기 좋습니다.
- **생먹이 사냥.** `g`로 풀어놓은 브라인슈림프(`~` 분홍색)는 가만히 가라앉는 알갱이와 달리 꿈틀꿈틀 도망다닙니다. 물고기가 다가오면 반대쪽으로 튀고, 물고기들은 알갱이보다 산 먹이를 우선 추격해요. 잡아먹으면 영양 만점(번식 가속 1.5배). 50초를 버틴 새우는 모래 속으로 숨어버립니다.
- **물고기마다 이름이 있어요.** 방울이, 통통이, 쏜살이… 태어날 때 이름을 받고, `i` 도감에서 나이와 먹은 먹이 수를 볼 수 있습니다. 클릭해서 만지면 이름을 부르며 도망가요.
- **가끔 귀한 손님이 옵니다.** 저 멀리 고래가 지나가고, 거북이가 놀러 오고, 문어는 먹물을 뿜고 사라집니다. 목격 기록은 도감에 남아요. (성질 급한 분들을 위한 이스터에그: `AQUARIUM_VISITOR=whale aquarium` 처럼 실행하면 손님이 자주 옵니다)
- **청소부들이 있어요.** 달팽이 `@`는 모래에 가라앉은 먹이를 향해 느릿느릿 기어가 먹어치우고, 게 `v(;,;)v`는 옆걸음으로 뽈뽈 다니다 가끔 멈춰서 집게를 번쩍 들어 흔듭니다. 물고기가 잠든 밤에도 일하죠.
- **어항이 저장됩니다.** 종료하면 `~/.aquarium.json`에 상태가 남고, 다음 실행에서 그대로 이어집니다. 자리를 비운 동안 태어난 물고기도 돌아오면 반겨줘요.
- **밤이 되면** 수조가 어두워지고 테두리에 별과 달이 뜹니다. 물고기들은 바닥 근처에서 천천히 잠들고(`z`), 해파리는 청록빛으로 발광해요. 저녁 7시~아침 7시이거나 터미널·시스템이 다크 테마면 자동으로 밤이 되고, `n` 키로 직접 바꿀 수도 있습니다.
